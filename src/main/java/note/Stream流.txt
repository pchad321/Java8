Stream流(针对集合)

流有三个部分组成：
1. 源
2. 零个或多个中间操作(中间操作返回一个Stream对象)
3. 终止操作(不返回Stream类型，可能不返回值，也可能返回其他类型的单个值)

流操作的分类
1. 惰性求值
2. 及早求值

不允许对一个流进行重复操作，也不允许操作一个已经关闭的流。

无限流(流中数据无限多)

内部迭代：使用流，可以并行执行

外部迭代：for循环，迭代器，一般只能串行执行

集合关注的是数据以及数据本身的存储

流关注的则是对数据的计算

分组：group by
分区：partition by(只会分成两类，true或者false)